[project]
name = "site"
version = "0.1.0"
description = "Website for confunnel Stata package using Quarto and nbstata Jupyter kernel"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyterlab>=4.4.9",
    "nbstata>=0.8.3",
]
